name: Deploy Application

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Deploy on VM
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          password: ${{ secrets.VM_PASSWORD }}
          port: 22
          script: |
            # Use a lock file to prevent parallel deployments
            (
              flock -n 200 || { echo "Deployment in progress. Exiting."; exit 1; }

              echo "Stopping pm2 process (if running)..."
              if pm2 describe gdg > /dev/null 2>&1; then
                pm2 stop gdg
              fi

              # Change to the deployment directory
              cd /var/www/gdg || { echo "Deployment directory not found"; exit 1; }
              
              echo "Deleting existing files except node_modules..."
              # Delete all files and directories except the node_modules folder.
              # (If your deployment folder is also a git repo, consider preserving the .git folder.)
              find . -maxdepth 1 -mindepth 1 ! -name "node_modules" -exec rm -rf {} +

              echo "Cloning repository to a temporary folder..."
              git clone https://github.com/praveentcom/gdg-community-kit.git /tmp/gdg-community-kit

              echo "Copying new files from the repository..."
              cp -r /tmp/gdg-community-kit/. .

              echo "Cleaning up temporary repository..."
              rm -rf /tmp/gdg-community-kit

              echo "Installing dependencies..."
              npm install

              echo "Building the application..."
              npm run build

              echo "Starting (or restarting) pm2 process..."
              if pm2 describe gdg > /dev/null 2>&1; then
                pm2 restart gdg
              else
                pm2 start npm --name "gdg" -- run start
              fi
            ) 200>/tmp/deploy.lock